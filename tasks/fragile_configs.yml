
--- 

- name: ensures prometheus dir exists
  file:
    path: "{{ docker_path }}/prometheus"
    state: directory
    
- name: copy prometheus configuration
  copy:
    src: "configs/fragile/prometheus.yml"
    dest: "{{ docker_path }}/prometheus/prometheus.yml"

